[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
addopts = --strict-markers
filterwarnings =
    ignore::DeprecationWarning

markers =
    integration: calls real Meta Graph API; requires META_ACCESS_TOKEN and META_RUN_LIVE_TESTS=1
